package com.dan.librocuentaskt.utils

class Utilitario {

/*
    fun obtenerMedioPago(codMedPago: String, arrayListTipoIngreso:ArrayList<Ingreso>): String? {
        var desc: String = EMPTY_STRING
        if (Constantes.listaMediosPago != null && !Constantes.listaMediosPago.isEmpty()) {
            for (medioPago in Constantes.listaMediosPago) {
                if (codMedPago == medioPago.getCodMedioPago()) {
                    desc = medioPago.getDesMedioPago()
                    break
                }
            }
        }
        return desc
    }*/
}